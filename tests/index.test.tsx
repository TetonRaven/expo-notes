import HomeScreen from "../app/(tabs)/index";

it('should do it', () => {
    //const homeScreen = new HomeScreen();
    //expect(homeScreen).toBeDefined();

    // var hs = HomeScreen()
    // hs.showNewNote();
});